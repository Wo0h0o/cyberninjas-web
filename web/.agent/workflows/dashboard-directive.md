---
description: Директива за Dashboard компонентите и Skill Tree имплементацията
---

# 📋 Dashboard Директива

> **ВАЖНО**: Този документ трябва да се актуализира при всяка корекция от потребителя относно имплементацията. След одобрение на промяна, директивата се обновява автоматично.

---

## 🎨 Дизайн Принципи

### Цветова Схема (Brand Colors)
```typescript
const BRAND = {
    yellow: '#FFFF00',        // Основен акцент
    yellowMuted: '#E6E600',   // Приглушен жълт
    green: '#22C55E',         // Добри примери
    greenBg: 'rgba(34, 197, 94, 0.15)',  // Фон за добри примери
    red: '#EF4444',           // Лоши примери
    redBg: 'rgba(239, 68, 68, 0.15)',     // Фон за лоши примери
    promptYellow: '#FFD93D',  // Промпт кутии
    promptBg: 'rgba(255, 217, 61, 0.1)', // Фон за промптове
    blue: '#3B82F6',          // Съвети (tips)
    blueBg: 'rgba(59, 130, 246, 0.1)',   // Фон за съвети
}
```

### Gradient Text
Заглавия и важни елементи използват gradient:
```css
background: linear-gradient(135deg, #FFFF00 0%, #FFD700 50%, #FFA500 100%);
-webkit-background-clip: text;
-webkit-text-fill-color: transparent;
```

### Skill Tree Node Visual States

Модулите имат ясно визуално разграничение според състоянието:

| Състояние | Stroke | Fill | Glow | Text | Икона |
|-----------|--------|------|------|------|-------|
| 🔒 **Locked** | `#2a2a2a` | `rgba(20,20,20,0.9)` | Няма | `#555555` | Lock icon (сив) |
| ⚪ **Available** | `#00D4FF` | `rgba(0,212,255,0.05)` | Cyan | `#AAAAAA` | Модулна икона (cyan) |
| 🔵 **Current** | `#00D4FF` (2.5px) | `rgba(0,212,255,0.15)` | Cyan + pulse | `#FFFFFF` | Модулна икона (cyan) |
| 🟡 **Completed** | `#FFD700` | `rgba(255,215,0,0.15)` | Gold/Orange | `#FFD700` | Модулна икона (gold) + ✓ badge |

**Визуални елементи:**
- **Checkmark badge**: Златен кръг с отметка в горния десен ъгъл на завършени модули
- **Lock icon**: Сив катинар в центъра на заключени модули
- **Pulse animation**: Пулсираща вътрешна рамка за текущия модул
- **Ripple effect**: Разширяващи се вълни за достъпни модули

### Comet Trail Colors

Цветът на comet trail-овете зависи от състоянието на свързаните модули:

| От → Към | Цвят на Trail |
|----------|---------------|
| Жълт (completed) → Жълт (completed) | 🟡 Златен (`#FFD700`) |
| Жълт (completed) → Син (available/current) | 🔵 Cyan (`#00D4FF`) |

Това се контролира чрез `isGoldPath` prop в `SkillPath.tsx`.

---

## 📝 Skill Tree Content Formatting

### Маркери за Визуално Форматиране

| Маркер | Резултат | Употреба |
|--------|----------|----------|
| `• Лош:` | Червена кутия с ✗ | Лош пример в сравнение |
| `• Добър:` | Зелена кутия с ✓ | Добър пример в сравнение |
| `💡` | Синя кутия | Съвети, принципи, ключови моменти |
| `Промпт:` ... `КРАЙ_ПРОМПТ` | Жълта кутия | Многоредови примерни промптове |
| `Роля — ...` | Full Gradient текст | Заглавия с тире (цял ред в gradient) |

### Многоредови Промптове

Промптовете се маркират с `Промпт:` в началото и `КРАЙ_ПРОМПТ` в края:

```
Промпт:
<системна_роля>
Ти си редактор на научни текстове.
</системна_роля>
<инструкции>
1. Анализирай текста.
2. Пренапиши го.
</инструкции>
КРАЙ_ПРОМПТ
```

Парсерът в `RichContent.tsx` събира всички редове между маркерите и ги показва в жълта кутия.

### Структура на Секции

Всеки модул има:
- `duration`: Примерно време за изпълнение
- `intro`: Въведителен текст
- `sections[]`: Масив от секции с `title` и `content`
- `keyTakeaways[]`: Ключови изводи

---

## 🧩 Компоненти

### Skill Tree (`components/skill-tree/`)

| Файл | Описание |
|------|----------|
| `SkillTree.tsx` | Главен компонент с визуализация на дървото |
| `SkillNode.tsx` | Индивидуален възел в дървото |
| `SkillPath.tsx` | SVG пътища между възлите |
| `NodeContent.tsx` | Съдържание на отворен модул |
| `RichContent.tsx` | **Парсер за форматиране на съдържание** |
| `ModuleContentBG.ts` | Българско съдържание на модулите |
| `AIPlayground.tsx` | Интегриран AI плейграунд |
| `TreeControls.tsx` | Контроли за zoom |
| `useModuleContent.ts` | Hook за зареждане на съдържание |

### RichContent Parser

Парсерът в `parseContent()` разпознава:
1. **Good/Bad примери** - `• Добър:` / `• Лош:`
2. **Tips** - редове с `💡` или `Съвет:`, `Важно:`
3. **Промптове** - `Промпт:` до `КРАЙ_ПРОМПТ`
4. **Списъци** - редове започващи с `•` или `1.`
5. **Subheadings** - формат `Термин: Подзаглавие`
6. **Full Gradient** - редове с ` — ` (em-dash)
7. **Headings** - кратки редове завършващи на `:`
8. **Paragraphs** - всичко останало

### Dashboard Layout

```
┌─────────────────────────────────────────┐
│  Sidebar (Desktop)  │   Main Content   │
│  - Navigation       │   - children     │
│  - User info        │                  │
│                     │                  │
└─────────────────────────────────────────┘
│         Bottom Nav (Mobile)            │
└─────────────────────────────────────────┘
```

---

## 🔧 Технически Принципи

### 1. Inline Styles vs CSS Classes
- **RichContent**: Използва inline styles за по-прецизен контрол
- **Dashboard Layout**: Използва Tailwind CSS класове

### 2. Responsive Design
- Desktop: Sidebar вляво (`lg:pl-60`)
- Mobile: Bottom navigation (`pb-20 lg:pb-0`)

### 3. Authentication
- `useAuth()` hook от `AuthContext`
- Redirect към `/login` ако няма потребител
- Loading state докато се проверява auth

### 4. Content Structure
- Съдържанието е в TypeScript обекти (`ModuleContentBG.ts`)
- Поддържа се лесна локализация
- Rich formatting чрез текстови маркери

---

## 📌 Правила за Актуализация

1. **При добавяне на нов маркер** → Актуализирай таблицата с маркери
2. **При промяна на цветова схема** → Актуализирай BRAND константите
3. **При нов компонент** → Добави в таблицата с компоненти
4. **При нов парсинг pattern** → Документирай в секция RichContent Parser

---

## 🚀 Бъдещи Подобрения

- [ ] Dark/Light theme toggle
- [ ] Progress tracking per module
- [ ] AI Playground integration в модулите
- [ ] Export на прогрес

---

*Последна актуализация: 2026-01-07*
